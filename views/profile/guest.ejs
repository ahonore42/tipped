<h1><%= currentUser.firstname %> viewing <%= userProfile.firstname %>'s page</h1>

<div class="row">
    <div class="col s4">
        <img class="profpic" src="<%= userProfile.pic %>" alt="profile pic">
    </div>
    <div>
        <p><strong>Name: </strong><%= userProfile.firstname %> <%= userProfile.lastname %></p>
        <p><strong>About: </strong><%= userProfile.bio %></p>
    </div>
</div>